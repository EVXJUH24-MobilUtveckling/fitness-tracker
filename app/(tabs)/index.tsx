/**
 * index.tsx - Startsidan (Home)
 *
 * Det här är första skärmen vi ser när vi öppnar appen.
 * Just nu är den ganska tom, men här kan vi senare lägga till
 * saker som snabbstatistik, senaste träningen, eller motiverande citat.
 */

import { StyleSheet, Text } from "react-native";

/**
 * HomeScreen - Huvudkomponenten för startsidan
 *
 * En enkel skärm som bara visar texten "Home".
 * Den här kan vi utöka senare med mer innehåll!
 */
export default function HomeScreen() {
  return <Text>Home</Text>;
}

// Styles kan läggas till här senare när vi vill styla skärmen
const styles = StyleSheet.create({});
